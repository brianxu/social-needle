<%= form_for :comment, @comment, :url => { :action => "create" } do |f| %>
  <div class="content">
    <%= f.text_area :content, :cols => 60, :rows => 4 %>
  </div>
  <div>
    <%= f.submit "Say it" %>
  </div>
<% end %>
